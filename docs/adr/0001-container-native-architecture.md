# ADR 0001: Container-native architecture

**Status:** Accepted

RedDock ships as a Linux container with Docker Compose as the primary local entry point. This makes host OS differences irrelevant and keeps runtime dependencies out of user machines.

